#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(pwd)"
QUALITY_BIN="$PROJECT_ROOT/vendor/bin/quality-checks"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [ -x "$QUALITY_BIN" ]; then
  exec "$QUALITY_BIN"
fi

exec "$SCRIPT_DIR/run-quality-checks.sh"
